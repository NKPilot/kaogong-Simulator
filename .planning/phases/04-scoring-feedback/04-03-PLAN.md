---
phase: 04-scoring-feedback
plan: 03
type: execute
wave: 2
depends_on:
  - 04-02
files_modified:
  - frontend/src/pages/ScoringResults/components/ScoringCard.tsx
  - frontend/src/pages/ScoringResults/components/CoverageDots.tsx
  - frontend/src/pages/ScoringResults/components/PendingCard.tsx
  - frontend/src/pages/ScoringResults/components/ErrorCard.tsx
autonomous: true
requirements:
  - SCORE-03
  - UI-05

must_haves:
  truths:
    - "CoverageDots renders colored dots (green/orange/red) for COVER/PARTIAL/MISS verdicts with status labels"
    - "ScoringCard displays question title, coverage fraction (e.g. 3/5 已覆盖), coverage dots, LLM feedback text, collapsible ASR transcript, and 重新评分 button"
    - "PendingCard shows Ant Design Skeleton with Spin indicator and 评分中... text"
    - "ErrorCard shows error icon, 评分失败 heading, and 点击重试 button"
    - "All components use inline CSS-in-JS styles consistent with project conventions"
    - "Card entry animation fades in with 300ms ease-out per UI-SPEC animation contract"
  artifacts:
    - path: "frontend/src/pages/ScoringResults/components/CoverageDots.tsx"
      provides: "Coverage dot visualization — 12px colored circles with status labels and point text"
      exports: ["default"]
    - path: "frontend/src/pages/ScoringResults/components/ScoringCard.tsx"
      provides: "Full scored question result card composing CoverageDots, Collapse, and re-score button"
      exports: ["default"]
    - path: "frontend/src/pages/ScoringResults/components/PendingCard.tsx"
      provides: "Skeleton loading state for in-progress scoring"
      exports: ["default"]
    - path: "frontend/src/pages/ScoringResults/components/ErrorCard.tsx"
      provides: "Error state card with retry button for failed scoring"
      exports: ["default"]
  key_links:
    - from: "ScoringCard.tsx"
      to: "CoverageDots.tsx"
      via: "coverage prop passed to CoverageDots component"
      pattern: "import CoverageDots"
    - from: "ScoringCard.tsx"
      to: "Ant Design Collapse"
      via: "ASR transcript collapsible section"
      pattern: "import.*Collapse.*from.*antd"
    - from: "ScoringCard.tsx"
      to: "ScoringResult type"
      via: "Props interface using ScoringResult"
      pattern: "from.*types/scoring"
---

<objective>
Create the four leaf components that visually render scoring results: CoverageDots (per-point dot visualization), ScoringCard (full scored result card), PendingCard (loading skeleton), and ErrorCard (failure state with retry).

Purpose: These are the visual building blocks that the ScoringResultsPage (Plan 04-04) composes into the card waterfall.
Output: Four presentational React components in pages/ScoringResults/components/
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-scoring-feedback/04-UI-SPEC.md
@.planning/phases/04-scoring-feedback/04-RESEARCH.md
@frontend/src/types/scoring.ts
@frontend/src/types/interview.ts

<interfaces>
<!-- Types and contracts the executor needs. Extracted from Plan 04-02 output. -->

From frontend/src/types/scoring.ts (created by Plan 04-02):
```typescript
export type ScoringStatus = 'not_started' | 'pending' | 'scored' | 'failed';
export type CoverageVerdict = 'COVER' | 'PARTIAL' | 'MISS';

export interface CoveragePoint {
  id: number;
  section: string;
  text: string;
  verdict: CoverageVerdict;
  evidence: string;
  reasoning: string;
}

export interface ScoringResult {
  questionIndex: number;
  questionId: string;
  questionTitle: string;
  status: ScoringStatus;
  transcript?: string;
  coverage?: CoveragePoint[];
  feedback?: string;
  coveredCount?: number;
  totalCount?: number;
  error?: string;
}
```

Design tokens from 04-UI-SPEC.md:
```
Spacing: xs=4px, sm=8px, md=16px, lg=24px, xl=32px
Typography:
  - Card Heading: 16px / 700 / 1.4 — question title per card
  - Score Display: 28px / 700 / 1.2 — coverage fraction
  - Body: 14px / 400 / 1.5 — feedback text, transcript
  - Label: 13px / 400 / 1.4 — coverage labels, metadata
Color:
  - Semantic Green #52C41A — COVER dot
  - Semantic Orange #FA8C16 — PARTIAL dot
  - Semantic Red #FF4D4F — MISS dot / error state
  - Accent #BE1E2D — primary button, coverage fraction number
  - Text Primary #262626 — body, headings
  - Text Secondary #8C8C8C — labels, metadata
  - Border #E8E8E8 — card borders, dividers
  - Card surface #FFFFFF
  - Page background #F5F5F5
Animation:
  - Card entry: fade in + translateY(8px→0), 300ms ease-out, 100ms stagger
  - Coverage dots: scale 0→1, 200ms ease-out
  - Reduced motion: all animations disabled
Component library: Ant Design v5 — Card, Typography, Collapse, Button, Skeleton, Spin, Space, Tag
```

Existing project pattern: All components use inline `style` objects (CSS-in-JS). No CSS modules, no styled-components, no Tailwind. Same as Phases 1-3.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CoverageDots + ScoringCard components</name>
  <files>frontend/src/pages/ScoringResults/components/CoverageDots.tsx, frontend/src/pages/ScoringResults/components/ScoringCard.tsx</files>

  <read_first>
    <file>frontend/src/types/scoring.ts</file>
    <file>.planning/phases/04-scoring-feedback/04-UI-SPEC.md</file>
    <file>frontend/src/pages/QuestionInterview/components/StatusTextBar.tsx</file>
    <file>frontend/src/pages/QuestionInterview/components/ProgressIndicator.tsx</file>
  </read_first>

  <action>
    Create directory: `frontend/src/pages/ScoringResults/components/`

    **Step 1 — Create CoverageDots.tsx:**

    Component signature: `export default function CoverageDots({ coverage }: { coverage: CoveragePoint[] })`
    Imports: CoveragePoint from '../../types/scoring' (adjust relative path to src/types/scoring.ts)
    Uses: `Typography.Text` from antd for labels

    Render logic:
    - Map over the `coverage` array
    - For each CoveragePoint, render a row containing:
      a. Colored dot: a `<div>` or `<span>` rendered as a 12px diameter circle (`width: 12, height: 12, borderRadius: '50%', display: 'inline-block'`)
      b. Status label: "已覆盖" (COVER), "部分覆盖" (PARTIAL), or "未覆盖" (MISS)
      c. Point text: `{point.section} — {point.text}` concatenated with em-dash
    - Dot colors per UI-SPEC:
      - COVER → `#52C41A` (green)
      - PARTIAL → `#FA8C16` (orange)
      - MISS → `#FF4D4F` (red)
    - Layout: each row uses `display: 'flex'`, `alignItems: 'center'`, `gap: 8` (sm spacing)
    - Row margin-bottom: 4px (xs spacing)
    - Label styles: fontSize 13, fontWeight 400, color '#8C8C8C' (secondary text), marginRight 4
    - Point text: fontSize 14, fontWeight 400, color '#262626' (primary text)
    - Container: marginTop 12 (after coverage fraction in parent)
    - Dot animation: apply `transition: 'transform 200ms ease-out'` and `transform: 'scale(1)'` (initial scale(0) handled by CSS animation on mount — use a useEffect with a timeout to flip state, or keep simple with no animation for v1)

    **Step 2 — Create ScoringCard.tsx:**

    Component signature: ScoringCardProps interface with result: ScoringResult, optional animationDelay?: number, onRescore: () => void. Default export function ScoringCard({ result, animationDelay = 0, onRescore }: ScoringCardProps).

    Imports: Card, Typography, Collapse, Button from 'antd'; ScoringResult from '../../types/scoring'; CoverageDots from './CoverageDots'
    Uses: Typography.Title (level={4}), Typography.Text, Card, Collapse, Button

    Card layout (top to bottom, per UI-SPEC Layout Contract):
    1. **Question title** — Typography.Title level={4} (renders as 16px/700 per Ant Design), content: result.questionTitle
    2. **Coverage fraction** — prominent display: `{result.coveredCount} / {result.totalCount} 已覆盖`
       - Styled as 28px fontSize, 700 fontWeight, lineHeight 1.2
       - The number (coveredCount) rendered in accent color #BE1E2D
       - The rest in text primary #262626
       - Centered or left-aligned within card body
    3. **CoverageDots** — `<CoverageDots coverage={result.coverage || []} />`
    4. **LLM Feedback** — section with "文字反馈" label:
       - Label in Typography.Text: fontSize 13, color '#8C8C8C' (secondary)
       - Feedback body in Typography.Text: fontSize 14, color '#262626', display as block paragraph
       - Separator: small margin above feedback section (16px)
    5. **Collapsible ASR transcript** — Ant Design Collapse component:
       - Single Panel with header="语音识别原文"
       - Default state: collapsed (`defaultActiveKey={[]}` or activeKey controlled to keep collapsed)
       - Panel content: result.transcript rendered in Typography.Text, fontSize 14, color '#262626'
       - Only render if result.transcript exists and is non-empty
    6. **重新评分 button** — Ant Design Button type="primary" (renders with accent #BE1E2D background):
       - Label: "重新评分"
       - onClick: calls onRescore
       - Positioned at card bottom
       - Add a small margin-top (16px) to separate from content above

    Card container styles (inline):
    - Card component with style prop: `marginBottom: 24` (lg spacing between waterfall cards)
    - Card body padding: 24px (md = 16 but cards deserve more breathing room)
    - Card width: 100% (within content area's max-width 960px)
    - Card entry animation:
      - `opacity: 0` initially, animated to `opacity: 1` via useEffect with timeout
      - `transform: 'translateY(8px)'` initially, animated to `translateY(0)`
      - `transition: 'opacity 300ms ease-out, transform 300ms ease-out'`
      - `transitionDelay: '{animationDelay}ms'` (for stagger effect)
      - On mount: use useEffect with requestAnimationFrame or setTimeout(0) to trigger the transition
      - Check `prefers-reduced-motion` and skip animation if set

    Section dividers: Use a simple border-top: '1px solid #E8E8E8' div between major sections (coverage fraction → dots → feedback → transcript → button area) with 16px vertical margin.

    **No external CSS files.** All styles are inline React.CSSProperties objects — consistent with Phases 1-3.
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "CoverageDots|ScoringCard" | grep -i error || echo "CoverageDots + ScoringCard type-check clean"</automated>
  </verify>

  <acceptance_criteria>
    - CoverageDots.tsx renders each CoveragePoint as a row with 12px colored dot + status label + point text
    - Dot colors: COVER=#52C41A (green), PARTIAL=#FA8C16 (orange), MISS=#FF4D4F (red)
    - ScoringCard.tsx displays question title in Typography.Title level={4}
    - ScoringCard.tsx displays coverage fraction in 28px/700 with coveredCount in accent #BE1E2D
    - ScoringCard.tsx includes Collapse component for ASR transcript, collapsed by default, header "语音识别原文"
    - ScoringCard.tsx has "重新评分" Button type="primary" at card bottom
    - Card entry animation: fade in + translateY(8px→0) 300ms ease-out with configurable delay
    - TypeScript compilation passes with no errors for both files
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Create PendingCard + ErrorCard components</name>
  <files>frontend/src/pages/ScoringResults/components/PendingCard.tsx, frontend/src/pages/ScoringResults/components/ErrorCard.tsx</files>

  <read_first>
    <file>frontend/src/types/scoring.ts</file>
    <file>.planning/phases/04-scoring-feedback/04-UI-SPEC.md</file>
    <file>frontend/src/pages/ScoringResults/components/ScoringCard.tsx</file>
  </read_first>

  <action>
    **Step 1 — Create PendingCard.tsx:**

    Component signature: PendingCardProps interface with questionTitle: string, optional animationDelay?: number. Default export function PendingCard({ questionTitle, animationDelay = 0 }: PendingCardProps).

    Imports: Card, Skeleton, Spin, Typography from 'antd'; no scoring types needed (receives plain props)

    Render per UI-SPEC Pending state:
    - Ant Design Card with same marginBottom: 24 as ScoringCard
    - Entry animation: same fade-in + translateY pattern as ScoringCard
    - Card content:
      a. Question title: Typography.Title level={4} showing `questionTitle`
      b. Loading area — centered layout:
         - Ant Design Spin component (inline spinner)
         - Typography.Text "评分中..." next to the spinner, fontSize 14, color '#8C8C8C'
         - Arranged with Space or display:'flex', alignItems:'center', gap:8
      c. Skeleton placeholder: Ant Design Skeleton active paragraph with 3 rows — gives visual weight to the pending state
    - Card body padding: 24px

    **Step 2 — Create ErrorCard.tsx:**

    Component signature:
    
      animationDelay?: number;
    Component signature: ErrorCardProps interface with questionTitle: string, optional errorMessage?: string, optional animationDelay?: number, onRetry: () => void. Default export function ErrorCard({ questionTitle, errorMessage, animationDelay = 0, onRetry }: ErrorCardProps).
    - Entry animation: fade-in 200ms ease-out (no translateY per UI-SPEC error animation contract)
    - Card content:
      a. Question title: Typography.Title level={4} showing `questionTitle` (same as other cards for visual consistency)
      b. Error indicator: CloseCircleOutlined icon rendered at fontSize 24, color '#FF4D4F' (red)
      c. "评分失败" heading: Typography.Text at fontSize 16, fontWeight 700, color '#FF4D4F' (red)
      d. Error detail (optional): if errorMessage is provided, show it in Typography.Text fontSize 13, color '#8C8C8C'
      e. "点击重试" button: Ant Design Button, type="default" (outlined style):
         - Label: "点击重试"
         - Style: `borderColor: '#FF4D4F'`, `color: '#FF4D4F'` (red outline, matching error theme)
         - onClick: calls onRetry
    - Card body padding: 24px

    Both components: use only inline styles (CSS-in-JS). No external CSS files.
    Both components: use `useEffect` + `prefers-reduced-motion` media query check for animation gating, same pattern as ScoringCard.
  </action>

  <verify>
    <automated>cd frontend && npx tsc --noEmit 2>&1 | grep -E "PendingCard|ErrorCard" | grep -i error || echo "PendingCard + ErrorCard type-check clean"</automated>
  </verify>

  <acceptance_criteria>
    - PendingCard.tsx renders Card with Skeleton active + Spin + "评分中..." text
    - PendingCard.tsx shows questionTitle in Typography.Title level={4}
    - ErrorCard.tsx renders CloseCircleOutlined icon in red #FF4D4F
    - ErrorCard.tsx shows "评分失败" heading at 16px/700 in red #FF4D4F
    - ErrorCard.tsx has "点击重试" Button with red outline (type="default", borderColor='#FF4D4F')
    - ErrorCard.tsx shows optional errorMessage when provided
    - Both components apply entry animation with reduced-motion check
    - TypeScript compilation passes with no errors for both files
  </acceptance_criteria>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Backend JSON → React render | LLM feedback text and ASR transcript displayed in DOM |
| User click → onRescore/onRetry callback | Triggers API call to re-score |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | ScoringCard, CoverageDots (XSS via LLM output) | accept | React's default JSX escaping prevents XSS. LLM feedback and transcript are rendered as text content, not dangerouslySetInnerHTML. Low risk — backend validates JSON structure before persisting. |
| T-04-11 | Denial of Service | ErrorCard (rapid retry clicks) | mitigate | Disable retry button during API call (pending state). The parent ScoringResultsPage manages the state transition to PendingCard on click, preventing double-submit. |
</threat_model>

<verification>
### TypeScript Compilation
- `cd frontend && npx tsc --noEmit` — all four components type-check

### Visual Verification (manual, after Plan 04-04 wiring)
- Coverage dots render correct colors for each verdict
- ScoringCard shows all sections (title, fraction, dots, feedback, collapse, button)
- PendingCard shows skeleton + spinner + "评分中..."
- ErrorCard shows error icon + "评分失败" + retry button
</verification>

<success_criteria>
- [ ] CoverageDots.tsx renders dots with correct green/orange/red colors per verdict
- [ ] ScoringCard.tsx displays all required sections: title, coverage fraction, dots, feedback, collapse, re-score button
- [ ] PendingCard.tsx renders Skeleton + Spin + "评分中..." text
- [ ] ErrorCard.tsx renders error icon + "评分失败" heading + "点击重试" button
- [ ] All four components use inline CSS-in-JS styles (no external CSS files)
- [ ] TypeScript compilation passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-scoring-feedback/04-03-SUMMARY.md`
</output>
