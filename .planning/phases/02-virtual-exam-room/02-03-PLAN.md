---
phase: 02
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
files_modified:
  - frontend/src/pages/ExamRoom/components/GuidanceToggle.tsx
  - frontend/src/pages/ExamRoom/components/TTSControls.tsx
  - frontend/src/pages/ExamRoom/components/CTAButton.tsx
  - frontend/src/pages/ExamRoom/index.tsx
autonomous: false
requirements:
  - FLOW-02
must_haves:
  truths:
    - "User hears TTS audio reading the interview guidance on page load"
    - "User can pause, resume, and replay the TTS audio"
    - "User can toggle guidance text visibility (hidden by default)"
    - "User can proceed from guidance to the first question via '开始答题' button"
    - "If TTS fails, user sees error text and can still read guidance via toggle"
    - "If browser blocks autoplay, user sees a play button to manually start audio"
  artifacts:
    - path: "frontend/src/pages/ExamRoom/components/GuidanceToggle.tsx"
      provides: "Expandable/collapsible guidance text panel"
      exports: "GuidanceToggle (default)"
      props: "paragraphs: string[]"
    - path: "frontend/src/pages/ExamRoom/components/TTSControls.tsx"
      provides: "Audio playback controls (play/pause/replay) with state display"
      exports: "TTSControls (default)"
      props: "playing, paused, ended, error, loading, onPlayPause, onReplay"
    - path: "frontend/src/pages/ExamRoom/components/CTAButton.tsx"
      provides: "Primary '开始答题' button that navigates to Phase 3"
      exports: "CTAButton (default)"
    - path: "frontend/src/pages/ExamRoom/index.tsx"
      provides: "Full ExamRoomPage orchestrator — loads guidance, calls TTS, composes all components"
      exports: "ExamRoomPage (default)"
  key_links:
    - from: "ExamRoomPage/index.tsx"
      to: "guidanceApi.ts"
      via: "fetchGuidance on mount"
    - from: "ExamRoomPage/index.tsx"
      to: "ttsApi.ts"
      via: "synthesizeSpeech with guidance text"
    - from: "ExamRoomPage/index.tsx"
      to: "RedBanner.tsx"
      via: "import RedBanner"
    - from: "ExamRoomPage/index.tsx"
      to: "ExaminerRow.tsx"
      via: "import ExaminerRow"
    - from: "ExamRoomPage/index.tsx"
      to: "GuidanceToggle.tsx"
      via: "import GuidanceToggle"
    - from: "ExamRoomPage/index.tsx"
      to: "TTSControls.tsx"
      via: "import TTSControls"
    - from: "ExamRoomPage/index.tsx"
      to: "CTAButton.tsx"
      via: "import CTAButton"
---

<objective>
Create the interactive exam room components and orchestrate the complete exam room page.

Purpose: Wire together all components from Plan 02 with API calls from Plan 01 to create the full virtual exam room experience. Users land on this page after selecting questions in Phase 1, see the exam room layout, hear TTS guidance audio, can toggle text visibility, and proceed to the first question.

Output:
- GuidanceToggle component (expandable/collapsible text panel)
- TTSControls component (play/pause/replay with state display)
- CTAButton component (navigates to first question)
- ExamRoomPage (orchestrates all components with full state management)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/atis/HE/interview-simulator/frontend/src/pages/ExamRoom/components/RedBanner.tsx
@/home/atis/HE/interview-simulator/frontend/src/pages/ExamRoom/components/ExaminerRow.tsx
@/home/atis/HE/interview-simulator/frontend/src/pages/ExamRoom/components/EntryAnimation.css
@/home/atis/HE/interview-simulator/frontend/src/api/guidanceApi.ts
@/home/atis/HE/interview-simulator/frontend/src/api/ttsApi.ts
@/home/atis/HE/interview-simulator/frontend/src/store/questionBankStore.ts
@/home/atis/HE/interview-simulator/frontend/src/router.tsx
@/home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-UI-SPEC.md
@/home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-RESEARCH.md

<interfaces>
From Plan 02 — available components:
```
RedBanner       — no props, full-width red strip with "2025公务员模拟面试"
ExaminerRow     — no props, renders 5 shuffled examiner images with labels
EntryAnimation  — CSS class ".exam-room-entry" for fade-in + scale-up
```

From Plan 02 — API modules:
```typescript
// guidanceApi.ts
fetchGuidance(questionCount: number): Promise<{title: string, paragraphs: string[]}>

// ttsApi.ts  
synthesizeSpeech(text: string): Promise<ArrayBuffer>
```

From questionBankStore.ts:
```typescript
selectedIds: string[]
questions: Question[]
```

From router.tsx:
```tsx
{ path: 'exam-room', element: <ExamRoomPage /> }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GuidanceToggle, TTSControls, and CTAButton components</name>
  <files>
    - frontend/src/pages/ExamRoom/components/GuidanceToggle.tsx (NEW)
    - frontend/src/pages/ExamRoom/components/TTSControls.tsx (NEW)
    - frontend/src/pages/ExamRoom/components/CTAButton.tsx (NEW)
  </files>

  <read_first>
    - /home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-UI-SPEC.md (C-03 GuidanceToggle, C-04 TTSControls, C-05 CTAButton specs, States tables, Copywriting Contract, Interactions I-03 through I-06)
    - /home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-RESEARCH.md (Pattern 2: Browser Autoplay Policy Handling)
  </read_first>

  <action>
    Create three React components:

    1. `frontend/src/pages/ExamRoom/components/GuidanceToggle.tsx`:
       - Props: `paragraphs: string[]` (guidance text paragraphs to display)
       - Internal state: `expanded: boolean` (initial false per D-10 / UI-SPEC C-03 "Collapsed (default)")
       - Button trigger: displays "查看引导语文字" when collapsed, "收起引导语文字" when expanded
       - Button uses `DownOutlined` icon when collapsed, `UpOutlined` icon when expanded (from `@ant-design/icons`)
       - Expandable panel: when expanded, renders each paragraph in `<Typography.Paragraph>` or `<p>` tag
       - Text container: white background, borderRadius 6px, padding 16px
       - Text style: 16px font, weight 400, line-height 1.6, color #262626
       - Max-width: 720px centered
       - Smooth transition: use CSS `max-height` transition with 300ms duration. The collapsed panel max-height is `0` with `overflow: hidden`. Expanded panel max-height is a large value (e.g., 500px) that accommodates all content.
       - Use `useState` for expand/collapse state
       - Interaction I-05: clicking toggle button expands/collapses with smooth height transition

    2. `frontend/src/pages/ExamRoom/components/TTSControls.tsx`:
       - Props:
         - `playing: boolean` — true when audio is playing
         - `paused: boolean` — true when audio is paused (not playing, but loaded)
         - `ended: boolean` — true when audio finished playing
         - `error: boolean` — true when TTS API failed
         - `loading: boolean` — true when TTS audio is being fetched
         - `onPlayPause: () => void` — handler for play/pause
         - `onReplay: () => void` — handler for replay
       - States per UI-SPEC TTSControls States table:
         - Loading: show `<Spin>` component, no buttons
         - Playing: show Pause icon button, status text "正在播放引导语..."
         - Paused: show Play icon button, status text "已暂停"
         - Ended: show Play icon button (replay available), status text "播放完毕"
         - Error: show text "语音播放失败，请查看下方文字" from Copywriting Contract, no buttons, no Spin
       - Play/Pause button: Ant Design `<Button>` with icon from `@ant-design/icons` (PlayCircleOutlined / PauseCircleOutlined)
       - Replay button: Ant Design `<Button>` with `ReloadOutlined` icon
       - Status text: Ant Design `<Typography.Text>` or `<span>` styled appropriately
       - Layout: centered row with buttons + status text, gap 16px
       - Follow UI-SPEC interaction I-03 (play/pause toggle), I-04 (replay), I-07 (ended state)

    3. `frontend/src/pages/ExamRoom/components/CTAButton.tsx`:
       - Props: `onClick: () => void` (handler for button click)
       - Ant Design `<Button>` type="primary" size="large"
       - Text: "开始答题" per UI-SPEC Copywriting Contract
       - Style: min-width 200px, centered
       - Margin-top: 32px
       - Never disabled in Phase 2 per UI-SPEC C-05 (always has pre-selected questions)
       - Interaction I-06: onClick handler
  </action>

  <acceptance_criteria>
    - GuidanceToggle accepts `paragraphs: string[]` prop
    - GuidanceToggle initially renders collapsed (text hidden, button says "查看引导语文字")
    - GuidanceToggle expands on click (button says "收起引导语文字", text visible)
    - GuidanceToggle uses UpOutlined/DownOutlined icons for collapse/expand state
    - TTSControls accepts all 6 boolean props + 2 handler function props
    - TTSControls shows Spin when loading=true
    - TTSControls shows play/pause + replay buttons when playing/paused/ended
    - TTSControls shows error text when error=true
    - TTSControls status text changes per state: "正在播放引导语..." / "已暂停" / "播放完毕"
    - CTAButton is an Ant Design primary large button with text "开始答题"
    - CTAButton accepts onClick prop
    - TypeScript compilation succeeds: `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <verify>
    <automated>
      cd /home/atis/HE/interview-simulator/frontend && npx tsc --noEmit 2>&1
    </automated>
  </verify>

  <done>
    Three interactive components created: GuidanceToggle (expandable/collapsible text panel), TTSControls (play/pause/replay with state display), CTAButton (navigation button). All props typed correctly. TypeScript compilation passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create ExamRoomPage orchestrator with full API integration</name>
  <files>
    - frontend/src/pages/ExamRoom/index.tsx (REPLACE existing placeholder)
  </files>

  <read_first>
    - /home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-UI-SPEC.md (Page-level States table, Interactions I-01 through I-07, Layout Structure, all component interfaces)
    - /home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-RESEARCH.md (Pattern 2: Browser Autoplay Policy Handling, Common Pitfalls: Pitfalls 1 and 4, Architecture Diagram data flow)
    - /home/atis/HE/interview-simulator/frontend/src/pages/ExamRoom/index.tsx (existing placeholder to replace)
    - /home/atis/HE/interview-simulator/frontend/src/store/questionBankStore.ts (for reading selected question count)
    - /home/atis/HE/interview-simulator/frontend/src/router.tsx (to confirm the route path for Phase 3 navigation)
  </read_first>

  <action>
    Replace the existing placeholder ExamRoomPage (`index.tsx`) with the full orchestrator:

    Import block:
    - `import { useEffect, useRef, useState } from 'react'`
    - `import { useNavigate } from 'react-router-dom'`
    - `import { Spin } from 'antd'`
    - `import { fetchGuidance } from '../../api/guidanceApi'`
    - `import { synthesizeSpeech } from '../../api/ttsApi'`
    - `import { useQuestionBankStore } from '../../store/questionBankStore'`
    - `import RedBanner from './components/RedBanner'`
    - `import ExaminerRow from './components/ExaminerRow'`
    - `import GuidanceToggle from './components/GuidanceToggle'`
    - `import TTSControls from './components/TTSControls'`
    - `import CTAButton from './components/CTAButton'`
    - `import './components/EntryAnimation.css'`

    State management:
    - `guidanceText: { title: string, paragraphs: string[] } | null` — guidance API response
    - `guidanceLoading: boolean` — true while fetching guidance text
    - `guidanceError: boolean` — true if guidance fetch failed
    - `audioBlob: Blob | null` — received TTS audio blob
    - `ttsError: boolean` — true if TTS synthesis failed
    - `audioLoading: boolean` — true while fetching TTS audio
    - `audioPlaying: boolean` — true while audio is playing
    - `audioPaused: boolean` — true when audio is paused
    - `audioEnded: boolean` — true when audio playback finished
    - `playBlocked: boolean` — true if browser autoplay policy blocked playback (NotAllowedError)

    On mount flow:
    1. Get `selectedIds` and `questions` from Zustand store via `useQuestionBankStore(s => ...)`
    2. Compute `selectedQuestions = questions.filter(q => selectedIds.includes(q.id))` and `questionCount = selectedQuestions.length`
    3. Call `fetchGuidance(questionCount)` — on success, set `guidanceText` and then call TTS with the same text
    4. TTS flow: `synthesizeSpeech(fullGuidanceText)` where fullGuidanceText is the joined paragraphs — per Pitfall 4 prevention: use exact same text from guidance response
    5. On TTS success: create Blob from ArrayBuffer, create Object URL, set audio element src, attempt `audio.play()`
    6. If `audio.play()` rejects with `NotAllowedError` (DOMException name "NotAllowedError"), set `playBlocked=true` and show manual play button overlay — per Pitfall 1 / Interaction I-02
    7. The audio element is created via `useRef<HTMLAudioElement>` — pre-create on mount
    8. Event handlers for audio: `onplay -> setAudioPlaying(true)`, `onpause -> setAudioPaused(true)`, `onended -> setAudioEnded(true)`

    Autoplay handling (Pitfall 1 mitigation):
    - Create the `<audio>` ref element on mount
    - Pre-fetch audio immediately (start on mount)
    - Attempt to play when blob URL is set
    - On `NotAllowedError`: show "点击播放" overlay (a large Play button centered in the page, with text "点击播放引导语")
    - On user clicks the play overlay: call `audio.play()` again (now with user gesture), hide overlay

    TTS error handling (D-16 / silent degrade):
    - If any TTS call fails, set `ttsError=true`
    - Show error text ("语音播放失败，请查看下方文字") via TTSControls
    - Guidance text is still accessible via GuidanceToggle — no information is lost

    Guidance error handling:
    - If guidance API fails, set `guidanceError=true`
    - Show fallback text per UI-SPEC Copywriting Contract: "引导语加载失败，请检查网络连接后重试"
    - Use Ant Design `<Result>` or simple error message

    Layout structure per UI-SPEC Layout Structure (top to bottom):
    1. Wrapper div with `className="exam-room-entry"` for the entry animation (D-11, C-06)
    2. Page inner wrapper: apply negative margins or padding override to counteract AppLayout Content padding (32px on left/right). Set `margin: '-32px'` to allow full-width banner, then inner content sections that need centering use their own max-width.
       - Alternative approach: set `marginLeft: -32px`, `marginRight: -32px` on the page wrapper
       - Inner sections that should be centered: ExaminerRow area, GuidanceToggle area, TTSControls area, CTAButton area — use `maxWidth: 960px` with `margin: '0 auto'`
    3. RedBanner component (full-width per C-01)
    4. Content area (max-width 960px, centered, padding-top for spacing):
       - ExaminerRow component
       - Spacer (e.g., 2xl = 48px gap)
       - Loading state: if guidanceLoading, show `<Spin>` with "正在加载引导语..."
       - Error state: if guidanceError, show error text
       - Ready state: 
         - GuidanceToggle with paragraphs
         - TTSControls
         - CTAButton
    5. Audio element: hidden `<audio ref={audioRef}>` (not rendered visually)

    Loading state behavior per UI-SPEC Page-level States:
    - Banner and examiners visible immediately (they don't need API data)
    - Only the TTS controls area shows loading spinner
    - Do NOT block page render on API fetches

    Navigation on CTA click (Interaction I-06):
    - Stop any playing audio: call `audioRef.current?.pause()` and `audioRef.current = null`
    - Navigate to Phase 3 route: `navigate('/exam-room/question/1')` (placeholder route for Phase 3)

    Use `useNavigate()` from react-router-dom for programmatic navigation.

    Per D-20: Frontend first fetches guidance text, then sends the same text to TTS API.
    Per D-15: Auto-play on page load. Full playback controls.
    Per D-16: Silent degrade on TTS failure, show text fallback.
  </action>

  <acceptance_criteria>
    - ExamRoomPage composes RedBanner, ExaminerRow, GuidanceToggle, TTSControls, CTAButton
    - On mount: fetches guidance text from API with user's selected question count
    - After guidance loaded: sends same text to TTS API for audio synthesis
    - Creates hidden HTMLAudioElement for playback
    - Auto-plays audio on load (with browser autoplay policy fallback)
    - Shows play overlay if browser blocks autoplay (NotAllowedError)
    - Shows TTSControls (play/pause/replay) after audio loads
    - Shows GuidanceToggle with guidance paragraphs
    - On "开始答题" click: stops audio, navigates to /exam-room/question/1
    - If TTS fails: shows error text, GuidanceToggle still works
    - If guidance API fails: shows error message
    - Does NOT block page render on API calls (banner + examiners visible immediately)
    - Wrapper uses exam-room-entry CSS class for entry animation
    - TypeScript compilation succeeds: `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <verify>
    <automated>
      cd /home/atis/HE/interview-simulator/frontend && npx tsc --noEmit 2>&1
    </automated>
  </verify>

  <done>
    Full exam room page orchestrator created. All components composed correctly. State management covers all page-level states (loading, ready, error, autoplay blocked). TTS auto-play, guidance toggle, and CTA navigation all wired. TypeScript compilation passes.
  </done>
</task>

</tasks>

<checkpoints>
<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification of exam room visual and audio</name>
  <what-built>
    The complete exam room page with: red banner, 5 examiner images (silhouettes), collapsible guidance text, TTS audio playback controls, entry animation, and "开始答题" button.
  </what-built>
  <how-to-verify>
    To verify this plan's output:

    Prerequisites:
    1. Ensure backend is running: `cd /home/atis/HE/interview-simulator/backend && uv run uvicorn app.main:app --reload`
    2. Ensure frontend is running: `cd /home/atis/HE/interview-simulator/frontend && npx vite --host localhost`
    3. Open http://localhost:5173 in a browser

    Verification steps:
    1. Select 3-4 questions on the question bank page (Phase 1)
    2. Click "开始面试" button
    3. Verify the exam room page loads with:
       - [ ] Entry animation (fade-in + scale-up) plays on mount (600ms)
       - [ ] Red banner with "2025公务员模拟面试" spans full width at the top
       - [ ] 5 examiner silhouette images displayed in a row with rounded corners
       - [ ] Center image labeled "主考官", side images labeled "考官"
       - [ ] Audio auto-plays or "点击播放" overlay appears (depending on browser autoplay policy)
    4. Click the guidance toggle button:
       - [ ] Button text changes from "查看引导语文字" to "收起引导语文字"
       - [ ] Guidance text paragraphs appear with smooth expansion
       - [ ] Text reads: welcome message + "本次面试共 N 道题" (N = selected count) + rules + "准备好了吗？"
    5. Test TTS controls:
       - [ ] Play/Pause button toggles audio
       - [ ] Replay button restarts audio from beginning
       - [ ] Status text updates: "正在播放引导语..." / "已暂停" / "播放完毕"
    6. Click "开始答题" button:
       - [ ] Audio stops
       - [ ] Page navigates (may show blank or error for Phase 3 route — expected since Phase 3 not built)
  </how-to-verify>
  <resume-signal>
    Type "approved" if the exam room renders correctly with all verified features. Or describe any issues found.
  </resume-signal>
</task>
</checkpoints>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend -> backend API | Guidance and TTS API calls cross network boundary |
| audio element -> browser audio subsystem | Audio playback handled entirely client-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-04 | Information Disclosure | TTS audio blob in frontend memory | accept | Audio blob is ephemeral (stored in component-local ref, revoked on unmount via URL.revokeObjectURL). No server-side data in audio content. |
</threat_model>

<verification>
1. TypeScript compilation passes: `cd frontend && npx tsc --noEmit`
2. All acceptance criteria from both tasks verified via TypeScript compilation
3. Human verification of visual layout and audio playback (checkpoint task)
4. Vite dev server starts without errors
5. Backend API endpoints respond (guidance text + TTS)
</verification>

<success_criteria>
- [ ] ExamRoomPage orchestrates all components with correct import wiring
- [ ] Guidance text fetched on mount, TTS called with exact same text (Pitfall 4 prevention)
- [ ] Audio auto-plays on page load with browser autoplay policy fallback
- [ ] Collapsible guidance text toggle is functional (hidden by default per D-10)
- [ ] "开始答题" button navigates to Phase 3 route
- [ ] TTS failure shows graceful error text (D-16), guidance text still accessible
- [ ] Guidance API failure shows error message
- [ ] Banner and examiner images visible immediately (not blocked by API fetches)
- [ ] Entry animation visible on page mount
- [ ] Human sign-off obtained via checkpoint
</success_criteria>

<output>
After completion, create `.planning/phases/02-virtual-exam-room/02-03-SUMMARY.md`
</output>
