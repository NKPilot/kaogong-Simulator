---
phase: 02
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/public/examiners/examiner-1.svg
  - frontend/public/examiners/examiner-2.svg
  - frontend/public/examiners/examiner-3.svg
  - frontend/public/examiners/examiner-4.svg
  - frontend/public/examiners/examiner-5.svg
  - frontend/src/api/guidanceApi.ts
  - frontend/src/api/ttsApi.ts
  - frontend/src/pages/ExamRoom/components/RedBanner.tsx
  - frontend/src/pages/ExamRoom/components/ExaminerRow.tsx
  - frontend/src/pages/ExamRoom/components/EntryAnimation.css
autonomous: true
requirements:
  - FLOW-01
  - UI-01
must_haves:
  truths:
    - "User sees a full-width red banner with '2025公务员模拟面试' text at the top of the exam room"
    - "User sees 5 examiner images arranged in a horizontal row with name labels beneath each"
    - "Center image is labeled '主考官', side images are labeled '考官'"
    - "Page has a fade-in + scale-up entry animation on mount"
    - "Frontend API modules exist for fetching guidance text and synthesizing TTS audio"
  artifacts:
    - path: "frontend/public/examiners/examiner-1.svg through examiner-5.svg"
      provides: "5 silhouette placeholder images for examiner display"
    - path: "frontend/src/api/guidanceApi.ts"
      provides: "fetchGuidance(questionCount) function"
      exports: "fetchGuidance"
    - path: "frontend/src/api/ttsApi.ts"
      provides: "synthesizeSpeech(text) function for TTS audio"
      exports: "synthesizeSpeech"
    - path: "frontend/src/pages/ExamRoom/components/RedBanner.tsx"
      provides: "Full-width red banner component"
      exports: "RedBanner (default)"
    - path: "frontend/src/pages/ExamRoom/components/ExaminerRow.tsx"
      provides: "5-image horizontal examiner display with labels"
      exports: "ExaminerRow (default)"
    - path: "frontend/src/pages/ExamRoom/components/EntryAnimation.css"
      provides: "@keyframes examRoomEntry animation"
  key_links:
    - from: "ExaminerRow.tsx"
      to: "frontend/public/examiners/*.svg"
      via: "Random selection from /examiners/ path"
    - from: "ExamRoomPage (Plan 03)"
      to: "guidanceApi.ts"
      via: "import fetchGuidance"
    - from: "ExamRoomPage (Plan 03)"
      to: "ttsApi.ts"
      via: "import synthesizeSpeech"
---

<objective>
Create the frontend static assets and base UI components for the virtual exam room.

Purpose: Build the visual foundation of the exam room that users see on page load. This includes 5 silhouette examiner placeholder images (D-02), the red banner (D-08), the examiner image row with proper labels (D-01, D-04), entry animation (D-11), and the API client modules for Phase 3 consumption.

Output:
- 5 SVG silhouette images in frontend/public/examiners/
- guidanceApi.ts and ttsApi.ts frontend API modules
- RedBanner, ExaminerRow, EntryAnimation CSS components
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-UI-SPEC.md
@/home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-RESEARCH.md
@/home/atis/HE/interview-simulator/frontend/src/layouts/AppLayout.tsx
@/home/atis/HE/interview-simulator/frontend/src/api/client.ts
@/home/atis/HE/interview-simulator/frontend/src/api/questionsApi.ts
@/home/atis/HE/interview-simulator/frontend/src/store/questionBankStore.ts

<interfaces>
From frontend/src/api/client.ts (pattern for API client modules):
```typescript
const API_BASE_URL = 'http://localhost:8000';

export async function fetchQuestions(): Promise<Response> {
  return fetch(`${API_BASE_URL}/api/questions`);
}
```

From frontend/src/store/questionBankStore.ts (for reading selected question count in Plan 03):
```typescript
export const useQuestionBankStore = create<QuestionBankStore>((set, get) => ({
  questions: Question[];
  selectedIds: string[];
  // ...
}));
```

From frontend/src/layouts/AppLayout.tsx (Content wrapper padding):
```tsx
<Content style={{ padding: '32px', maxWidth: 960, margin: '0 auto', width: '100%' }}>
  <Outlet />
</Content>
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create 5 silhouette placeholder SVGs and frontend API modules</name>
  <files>
    - frontend/public/examiners/examiner-1.svg (NEW)
    - frontend/public/examiners/examiner-2.svg (NEW)
    - frontend/public/examiners/examiner-3.svg (NEW)
    - frontend/public/examiners/examiner-4.svg (NEW)
    - frontend/public/examiners/examiner-5.svg (NEW)
    - frontend/src/api/guidanceApi.ts (NEW)
    - frontend/src/api/ttsApi.ts (NEW)
  </files>

  <read_first>
    - /home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-RESEARCH.md (Code Examples section: Frontend API pattern, Silhouette SVG section)
    - /home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-UI-SPEC.md (Copywriting Contract for API format, examiner labels)
    - /home/atis/HE/interview-simulator/frontend/src/api/client.ts (API client module pattern to replicate)
    - /home/atis/HE/interview-simulator/frontend/src/api/questionsApi.ts (facade module pattern to replicate)
  </read_first>

  <action>
    Create 7 files:

    1. Five SVG silhouette files at `frontend/public/examiners/examiner-{1-5}.svg`:
       - Each is a valid standalone SVG (svg xmlns, viewBox, etc.)
       - Shows a human silhouette (shoulders/head outline) with suit jacket shape — per D-06 (dark suit, neutral, light gray background)
       - Fill color: dark gray (#333333) for the silhouette
       - Background: light gray (#F0F0F0) rectangle — per D-06 (浅灰背景)
       - Size: viewBox "0 0 200 240" (portrait proportion, ~200x240)
       - Each file has slight variation in posture/outline so they are subtly distinct while all being silhouette/轮廓风格 — per D-02
       - Simple enough to be ~1KB each with clean SVG markup
       - Include the desk+nameplate in the image per D-03: draw a dark horizontal bar at bottom (desk) with a lighter rectangle above it (nameplate)
       - Nameplate text should be a generic placeholder since actual labels ("主考官"/"考官") are rendered by the frontend ExaminerRow component
       - SVG should not contain text labels — labels come from frontend per D-04

    2. `frontend/src/api/guidanceApi.ts`:
       - Import `API_BASE_URL` from `./client` (reuse the existing constant pattern)
       - Export function `fetchGuidance(questionCount: number): Promise<{title: string, paragraphs: string[]}>`:
         - Fetches `GET ${API_BASE_URL}/api/interview/guidance?question_count=${questionCount}`
         - Throws error with message if response not OK
         - Returns parsed JSON as typed response
       - Follow same error handling pattern as questionsApi.ts

    3. `frontend/src/api/ttsApi.ts`:
       - Define `API_BASE_URL` same as client.ts (`http://localhost:8000`)
       - Export function `synthesizeSpeech(text: string): Promise<ArrayBuffer>`:
         - POST to `${API_BASE_URL}/api/tts/synthesize`
         - Body: `JSON.stringify({ text, voice: 'longxiaocheng_v2' })` per D-14
         - Content-Type: application/json
         - If response not OK, throw Error with descriptive message
         - Return `response.arrayBuffer()` on success

    Per D-20: separate API modules for text guidance and TTS. Per UI-SPEC API Contracts section.
  </action>

  <acceptance_criteria>
    - 5 SVG files exist in frontend/public/examiners/ with non-identical content
    - Each SVG has viewBox, silhouette figure, suit shape, light gray background, desk nameplate
    - SVGs contain NO text labels (labels rendered by frontend)
    - guidanceApi.ts exports `fetchGuidance` function
    - guidanceApi.ts constructs the correct URL path `/api/interview/guidance?question_count=N`
    - ttsApi.ts exports `synthesizeSpeech` function
    - ttsApi.ts POSTs to `/api/tts/synthesize` with JSON body containing `text` and `voice: 'longxiaocheng_v2'`
    - ttsApi.ts returns Promise&lt;ArrayBuffer&gt;
    - TypeScript compilation succeeds: `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <verify>
    <automated>
      # Verify SVG files exist and have expected structure
      for i in 1 2 3 4 5; do
        file="/home/atis/HE/interview-simulator/frontend/public/examiners/examiner-${i}.svg"
        [ -f "$file" ] || (echo "MISSING: $file" && exit 1)
        grep -q 'viewBox' "$file" || (echo "NO viewBox in $file" && exit 1)
        grep -q 'svg' "$file" || (echo "NO svg tag in $file" && exit 1)
        # Verify no Chinese text labels in SVGs (labels come from frontend)
        grep -cP '[\x{4e00}-\x{9fff}]' "$file" && (echo "SVG $file contains Chinese text labels -- should be in frontend component" && exit 1) || true
        echo "SVG $i OK"
      done
      echo "SVG directory OK"

      # Verify TypeScript compiles with new API modules
      cd /home/atis/HE/interview-simulator/frontend && npx tsc --noEmit 2>&1 | head -20
      echo "TypeScript compilation OK"
    </automated>
  </verify>

  <done>
    5 SVG silhouette files created in public/examiners/ with proper structure. Frontend API modules for guidance and TTS created with correct endpoint URLs and error handling. TypeScript compiles without errors.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create RedBanner, ExaminerRow components and EntryAnimation CSS</name>
  <files>
    - frontend/src/pages/ExamRoom/components/RedBanner.tsx (NEW)
    - frontend/src/pages/ExamRoom/components/ExaminerRow.tsx (NEW)
    - frontend/src/pages/ExamRoom/components/EntryAnimation.css (NEW)
  </files>

  <read_first>
    - /home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-UI-SPEC.md (C-01 RedBanner, C-02 ExaminerRow specs, colors, layout, typography)
    - /home/atis/HE/interview-simulator/frontend/src/layouts/AppLayout.tsx (padding that needs to be overridden for full-width banner)
  </read_first>

  <action>
    Create three files:

    1. `frontend/src/pages/ExamRoom/components/RedBanner.tsx`:
       - Default export React functional component
       - No props (static component per C-01)
       - Full-width div: `background: '#BE1E2D'`, `height: 64px`, `width: '100%'`
       - Use `position: 'relative'`, `left: 0`, `right: 0` to extend beyond parent padding
       - If using negative margin approach: `marginLeft: negative-32px`, `marginRight: negative-32px` (to counteract AppLayout's 32px Content padding)
       - Text "2025公务员模拟面试" centered: Ant Design Typography Title level=3 or direct h1-style
       - Text: 28px (the value from research recommendation), 700 weight, white color, centered, letter-spacing appropriate
       - Font based on Banner display role in Typography table of UI-SPEC

    2. `frontend/src/pages/ExamRoom/components/ExaminerRow.tsx`:
       - Default export React functional component
       - No props (self-contained — selects images internally)
       - Define constants: `EXAMINER_COUNT = 5`, `EXAMINER_PATH = '/examiners'`
       - Array of examiner indices: `['examiner-1.svg', 'examiner-2.svg', 'examiner-3.svg', 'examiner-4.svg', 'examiner-5.svg']`
       - Shuffle the array on component mount using Fisher-Yates or random sort
         - Use `useMemo` with empty deps to compute shuffled list once on mount
       - Render a styled div: `display: 'flex'`, `justifyContent: 'center'`, `gap: 16px`, `maxWidth: 960px`, `margin: '24px auto 0'`
       - For each examiner image (from shuffled list):
         - Wrapped in a div per image
         - `<img>` with width ~160px, `object-fit: cover`, `borderRadius: 8px`
         - Beneath each image: name label text, 14px weight 400, centered, color `#262626`
         - Center image (index 2 after shuffle) labeled "主考官", all others "考官" per D-04
       - Add subtle hover effect per UI-SPEC: `transform: 'scale(1.02)'` on image wrapper hover via CSS transition 200ms
       - Empty state: if examiners directory has zero images, render 5 placeholder divs (gray rounded rectangles) with labels. But since SVGs are bundled, this is an edge case.
       - Use `useMemo` + shuffle logic at module level, or inside component

    3. `frontend/src/pages/ExamRoom/components/EntryAnimation.css`:
       - Define `@keyframes examRoomEntry`:
         - from: `opacity: 0`, `transform: scale(0.98)`
         - to: `opacity: 1`, `transform: scale(1)`
       - Duration 600ms, ease-out timing function, forwards fill mode
       - Class name `.exam-room-entry`
       - This matches the pattern from Research.md Code Examples

    Naming conventions per D-04: center image "主考官", side images "考官".
    Per D-08: Red banner is the top element. Banner is full-width; AppLayout Content padding (32px) must be counteracted for the banner.
    Per C-01: Banner is static 64px height, no interaction.
    Per C-02: Images from public/examiners/, random 5 selection on page load (here: shuffle all 5).
    Per D-05: No specific image dimensions — use CSS object-fit to crop (cover).
    Per D-11: Entry animation via CSS @keyframes (directive implemented here, applied in Plan 03's ExamRoomPage).
  </action>

  <acceptance_criteria>
    - RedBanner.tsx exports a default React component
    - RedBanner renders "2025公务员模拟面试" text in white on #BE1E2D background
    - RedBanner is 64px tall, spans full width (negative margins to counteract AppLayout padding)
    - ExaminerRow.tsx exports a default React component
    - ExaminerRow renders 5 img elements with src paths starting with /examiners/examiner-
    - ExaminerRow has 5 name labels: center one "主考官", others "考官"
    - EntryAnimation.css exports .exam-room-entry class with @keyframes examRoomEntry animation
    - Animation duration is 600ms with ease-out
    - TypeScript compilation succeeds: `cd frontend && npx tsc --noEmit` exits 0
  </acceptance_criteria>

  <verify>
    <automated>
      cd /home/atis/HE/interview-simulator/frontend && npx tsc --noEmit 2>&1
    </automated>
  </verify>

  <done>
    RedBanner, ExaminerRow components and EntryAnimation CSS created. All components export properly and TypeScript compiles without errors. Components are ready for use by Plan 03 ExamRoomPage orchestrator.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend -> public/ directory | Static files served directly by Vite dev server, no API involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | public/examiners/ SVG files | accept | SVGs are static files bundled with the app. No user uploads in v1. Low-value target. |
</threat_model>

<verification>
1. All SVG files exist and are valid XML with viewBox
2. TypeScript compilation passes (`npx tsc --noEmit`)
3. Vite dev server can start: `cd frontend && npx vite --host localhost`
4. Components render without runtime errors (verify via browser in Plan 03)
</verification>

<success_criteria>
- [ ] 5 unique silhouette SVG placeholder images in public/examiners/
- [ ] Guidance and TTS API client modules created with correct endpoint paths
- [ ] RedBanner renders full-width span with correct color, text, and height
- [ ] ExaminerRow renders 5 images with correct labels (center "主考官", sides "考官")
- [ ] Entry animation CSS defined for fade-in + scale-up at 600ms
- [ ] TypeScript compilation passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/02-virtual-exam-room/02-02-SUMMARY.md`
</output>
