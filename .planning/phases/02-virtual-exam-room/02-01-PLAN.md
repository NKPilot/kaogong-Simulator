---
phase: 02
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/routers/guidance.py
  - backend/app/services/guidance_service.py
  - backend/app/routers/tts.py
  - backend/app/services/tts_service.py
  - backend/app/main.py
  - backend/pyproject.toml
autonomous: true
requirements:
  - FLOW-02
user_setup:
  - service: dashscope
    why: "TTS audio synthesis for guidance playback"
    env_vars:
      - name: DASHSCOPE_API_KEY
        source: "DashScope console -> API Keys (https://dashscope.aliyun.com)"
must_haves:
  truths:
    - "Backend serves guidance text with dynamic question count via GET /api/interview/guidance"
    - "Backend proxies TTS requests to DashScope, streaming audio back to frontend"
    - "Backend CORS allows both GET and POST methods"
    - "DashScope API key is never exposed to the frontend"
  artifacts:
    - path: "backend/app/routers/guidance.py"
      provides: "GET /api/interview/guidance endpoint"
      exports:
        - "router (APIRouter)"
        - "get_guidance(question_count) -> GuidanceResponse"
    - path: "backend/app/services/guidance_service.py"
      provides: "Guidance text generation with dynamic question_count substitution"
      exports:
        - "generate_guidance_text(question_count: int) -> GuidanceContent"
    - path: "backend/app/routers/tts.py"
      provides: "POST /api/tts/synthesize endpoint, input validation (max 500 chars)"
      exports:
        - "router (APIRouter)"
        - "synthesize(request: TTSRequest) -> StreamingResponse"
    - path: "backend/app/services/tts_service.py"
      provides: "DashScope SpeechSynthesizer wrapper"
      exports:
        - "synthesize_speech(text: str, voice: str) -> bytes"
    - path: "backend/app/main.py"
      provides: "CORS allow_methods=['GET','POST'], routers imported"
    - path: "backend/pyproject.toml"
      provides: "dashscope>=1.25.11 dependency"
  key_links:
    - from: "backend/app/main.py"
      to: "backend/app/routers/guidance.py"
      via: "app.include_router(guidance.router)"
    - from: "backend/app/main.py"
      to: "backend/app/routers/tts.py"
      via: "app.include_router(tts.router)"
    - from: "backend/app/routers/tts.py"
      to: "backend/app/services/tts_service.py"
      via: "import synthesize_speech"
---

<objective>
Create the backend API layer for Phase 2: guidance text endpoint and TTS audio synthesis proxy.

Purpose: Provide the server-side API endpoints that the frontend exam room consumes. The guidance endpoint returns structured welcome text with dynamic question count. The TTS endpoint proxies calls to DashScope SpeechSynthesizer, keeping the API key server-side per D-12, and streams audio back.

Output:
- `GET /api/interview/guidance?question_count=N` returning structured JSON with title and paragraphs
- `POST /api/tts/synthesize` accepting text + voice, streaming MP3 audio via chunked transfer encoding
- DashScope `dashscope>=1.25.11` dependency added to pyproject.toml
- CORS middleware updated to allow POST in addition to GET
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/atis/HE/interview-simulator/backend/app/main.py
@/home/atis/HE/interview-simulator/backend/app/routers/questions.py
@/home/atis/HE/interview-simulator/backend/app/services/question_service.py
@/home/atis/HE/interview-simulator/backend/pyproject.toml
@/home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-RESEARCH.md
@/home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-UI-SPEC.md

<interfaces>
From backend/app/routers/questions.py (FastAPI router pattern to replicate):
```python
from fastapi import APIRouter, HTTPException
router = APIRouter(tags=["questions"])
app.include_router(questions.router)  # in main.py
```

From backend/app/main.py (CORS config to update):
```python
allow_methods=["GET"],  # Must change to ["GET", "POST"]
```

From backend/pyproject.toml (add dashscope):
```toml
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.34.0",
    "pydantic>=2.10.0",
]
```

From UI-SPEC (API contracts):
- GET /api/interview/guidance?question_count=N returns `{ "title": "面试说明", "paragraphs": ["...", "..."] }`
- POST /api/tts/synthesize accepts `{ "text": "...", "voice": "longxiaocheng_v2" }`, returns audio/mpeg stream
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create guidance service and API endpoint</name>
  <files>
    - backend/app/services/guidance_service.py (NEW)
    - backend/app/routers/guidance.py (NEW)
  </files>

  <read_first>
    - /home/atis/HE/interview-simulator/backend/app/routers/questions.py (FastAPI router pattern to replicate)
    - /home/atis/HE/interview-simulator/backend/app/services/question_service.py (service layer pattern to replicate)
    - /home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-UI-SPEC.md (guidance text copy in Copywriting Contract section)
  </read_first>

  <action>
    Create two files:

    1. `backend/app/services/guidance_service.py`:
       - Module-level `_GUIDANCE_TEMPLATE` dict with `title` ("面试说明") and `paragraphs` list of 4 strings matching the UI-SPEC copywriting contract
       - Paragraph strings using `{count}` placeholder instead of hardcoded number
       - Export function `generate_guidance_text(question_count: int) -> dict` that returns `{"title": ..., "paragraphs": [formatted strings with {count} replaced]}`
       - Format all 4 paragraph strings with the question_count parameter (str.format or similar)
       - Follow the same module-level pattern as question_service.py (no classes, just functions + module-level constants)

    2. `backend/app/routers/guidance.py`:
       - Create APIRouter with `tags=["interview"]`
       - Import `generate_guidance_text` from services.guidance_service
       - Define Pydantic `GuidanceResponse` model: `title: str`, `paragraphs: List[str]`
       - Define `@router.get("/api/interview/guidance", response_model=GuidanceResponse)`
       - Endpoint function `get_guidance(question_count: int = Query(...))`:
         - Uses `Query(..., alias="question_count")` to accept query param
         - Calls `generate_guidance_text(question_count)`
         - Returns GuidanceResponse instance
       - Follow same APIRouter pattern as questions.py

    Per D-18: endpoint is GET with query parameter. Per D-20: this API is separate from TTS. Per UI-SPEC: the JSON format has `title` + `paragraphs[]`.
  </action>

  <acceptance_criteria>
    - guidance_service.py exists and exports `generate_guidance_text(question_count)` function
    - guidance.py exists and exports `router` (APIRouter)
    - GuidanceResponse Pydantic model has `title: str` and `paragraphs: List[str]`
    - `GET /api/interview/guidance?question_count=3` returns JSON with `title` and `paragraphs` array of 4 strings
    - Paragraph 2 contains "3 道题" when question_count=3, "4 道题" when question_count=4
    - All 4 paragraph strings match the UI-SPEC Copywriting Contract exactly (modulo {count} substitution)
  </acceptance_criteria>

  <verify>
    <automated>
      cd /home/atis/HE/interview-simulator/backend && python3 -c "
from app.services.guidance_service import generate_guidance_text
result = generate_guidance_text(3)
assert result['title'] == '面试说明'
assert len(result['paragraphs']) == 4
assert '3 道题' in result['paragraphs'][1]
result4 = generate_guidance_text(4)
assert '4 道题' in result4['paragraphs'][1]
print('guidance_service OK')

import json
from app.routers.guidance import GuidanceResponse
r = GuidanceResponse(**result)
assert r.title == '面试说明'
assert len(r.paragraphs) == 4
print('guidance router model OK')
"
    </automated>
  </verify>

  <done>
    Guidance service generates correct text with dynamic question count. Pydantic model validates the response shape. Both Python files pass import and functional tests.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create TTS service, API endpoint, update CORS and dependencies</name>
  <files>
    - backend/app/services/tts_service.py (NEW)
    - backend/app/routers/tts.py (NEW)
    - backend/app/main.py (MODIFY)
    - backend/pyproject.toml (MODIFY)
  </files>

  <read_first>
    - /home/atis/HE/interview-simulator/backend/app/main.py (CORS config, router registration, lifespan)
    - /home/atis/HE/interview-simulator/backend/pyproject.toml (dependency list format)
    - /home/atis/HE/interview-simulator/.planning/phases/02-virtual-exam-room/02-RESEARCH.md (TTS service code example in Code Examples section, StreamingResponse pattern in Architecture Patterns)
  </read_first>

  <action>
    Create two new files and modify two existing files:

    1. `backend/app/services/tts_service.py`:
       - Import `os`, `dashscope`, and `SpeechSynthesizer` from `dashscope.audio.tts_v2`
       - Set `dashscope.api_key` from `os.environ.get("DASHSCOPE_API_KEY", "")` at module level
       - Export function `synthesize_speech(text: str, voice: str = "longxiaocheng_v2") -> bytes`:
         - Validates text length is <= 500 characters (raise ValueError with message "Text too long" if exceeded) — per security threat mitigation for TTS text injection (Tampering)
         - Creates `SpeechSynthesizer(model="cosyvoice-v2", voice=voice, format="mp3", sample_rate=22050, rate=1.0)`
         - Calls `synthesizer.call(text)` and returns bytes
       - Voice default is "longxiaocheng_v2" per D-14 (male formal, 稳重、播音感)
       - Use Research.md's code example pattern

    2. `backend/app/routers/tts.py`:
       - Import `APIRouter`, `HTTPException`, `Query` from fastapi
       - Import `BaseModel`, `Field` from pydantic
       - Import `StreamingResponse` from fastapi.responses
       - Import `asyncio`
       - Import `synthesize_speech` from app.services.tts_service
       - Create APIRouter with `tags=["tts"]`
       - Define Pydantic `TTSRequest`: `text: str = Field(..., max_length=500)`, `voice: str = "longxiaocheng_v2"`
         - `max_length=500` is the security mitigation per threat T-02-02
       - Create async generator `audio_chunk_generator(text, voice)`:
         - Calls `await asyncio.to_thread(synthesize_speech, text, voice)` to run blocking SDK call in thread pool
         - Yields bytes in 8192 chunks
       - Define `@router.post("/api/tts/synthesize")`:
         - Accepts `TTSRequest` body
         - Returns `StreamingResponse(audio_chunk_generator(...), media_type="audio/mpeg", headers={"Cache-Control": "no-cache", "Accept-Ranges": "bytes"})`
         - Catches exceptions (ValueError for input validation, Exception for DashScope errors) and raises HTTPException with status 502 for synthesis failures

    3. `backend/app/main.py` (MODIFY):
       - Add import: `from app.routers import guidance, tts`
       - Add `app.include_router(guidance.router)` and `app.include_router(tts.router)` after existing router registrations
       - Change CORS `allow_methods` from `["GET"]` to `["GET", "POST"]`
       - Add startup warning: log a warning if `DASHSCOPE_API_KEY` env var is not set (check `os.environ.get("DASHSCOPE_API_KEY")`), but do not crash — per D-16 graceful degradation requirement

    4. `backend/pyproject.toml` (MODIFY):
       - Add `"dashscope>=1.25.11"` to the dependencies list
  </action>

  <acceptance_criteria>
    - tts_service.py exists with `synthesize_speech(text, voice)` exported function
    - tts_service.synthesize_speech raises ValueError for text > 500 chars
    - tts.py exists with APIRouter for POST /api/tts/synthesize
    - TTSRequest model has `text: str` with max_length=500 and `voice: str` with default "longxiaocheng_v2"
    - main.py imports guidance and tts routers and calls app.include_router for both
    - main.py CORS allow_methods is `["GET", "POST"]`
    - main.py logs warning if DASHSCOPE_API_KEY is not set (but does not crash)
    - pyproject.toml includes `dashscope>=25.11` in dependencies
  </acceptance_criteria>

  <verify>
    <automated>
      cd /home/atis/HE/interview-simulator/backend && python3 -c "
# 1. Verify TTS service module imports
from app.services.tts_service import synthesize_speech
import inspect
sig = inspect.signature(synthesize_speech)
assert 'text' in sig.parameters
assert 'voice' in sig.parameters
print('tts_service signature OK')

# 2. Verify input validation
try:
    synthesize_speech('x' * 501)
    assert False, 'Should have raised ValueError'
except ValueError as e:
    assert 'long' in str(e)
print('tts_service validation OK')

# 3. Verify TTSRequest model
from app.routers.tts import TTSRequest
r = TTSRequest(text='你好', voice='longxiaocheng_v2')
assert r.text == '你好'
assert r.voice == 'longxiaocheng_v2'
assert r.model_fields['text'].max_length == 500
print('TTSRequest model OK')

# 4. Verify pyproject.toml has dashscope
import tomllib
with open('pyproject.toml', 'rb') as f:
    data = tomllib.load(f)
deps = data['project']['dependencies']
assert any('dashscope' in d for d in deps), 'dashscope not in dependencies'
print('pyproject.toml dashscope OK')

# 5. Verify main.py imports
from app.main import app
routes = [r.path for r in app.routes]
assert '/api/interview/guidance' in routes
assert '/api/tts/synthesize' in routes
print('Routes registered OK')

# 6. Verify CORS allows POST
from starlette.middleware.cors import CORSMiddleware
for middleware in app.user_middleware:
    if middleware.cls == CORSMiddleware:
        opts = middleware.options
        assert 'POST' in opts['allow_methods'], f'POST not in {opts[\"allow_methods\"]}'
        print('CORS POST allowed OK')
        break

print('ALL PLAN 01 TASK 2 CHECKS PASSED')
"
    </automated>
  </verify>

  <done>
    TTS synthesis service implemented with DashScope SDK, TTS endpoint serves streaming audio via StreamingResponse, CORS allows POST, dashscope dependency added to pyproject.toml, API key warning logged on startup without crash.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| frontend -> backend | DashScope API key must never cross this boundary |
| backend -> DashScope | API key used server-side only |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | POST /api/tts/synthesize | mitigate | DashScope API key set via env var on server only, never sent in frontend responses. Key is set in tts_service.py module-level from `os.environ`. |
| T-02-02 | Tampering | POST /api/tts/synthesize request body | mitigate | TTSRequest.text has `max_length=500` via Pydantic Field validation. tts_service.synthesize_speech also validates length < 500 and raises ValueError. |
</threat_model>

<verification>
1. All acceptance criteria verified via automated Python assertions
2. `cd /home/atis/HE/interview-simulator/backend && uv run uvicorn app.main:app` boots without import errors
3. `curl http://localhost:8000/api/interview/guidance?question_count=3` returns valid JSON
4. Backend logs warning if DASHSCOPE_API_KEY not set (graceful degradation per D-16)
</verification>

<success_criteria>
- [ ] Backend serves `GET /api/interview/guidance?question_count=N` returning structured JSON matching UI-SPEC copy
- [ ] Backend accepts `POST /api/tts/synthesize` and streams audio
- [ ] CORS allows POST from localhost:5173 and 127.0.0.1:5173
- [ ] Input text length limited to 500 characters (security)
- [ ] API key never exposed to frontend
- [ ] Server boots without DASHSCOPE_API_KEY (graceful degradation)
</success_criteria>

<output>
After completion, create `.planning/phases/02-virtual-exam-room/02-01-SUMMARY.md`
</output>
