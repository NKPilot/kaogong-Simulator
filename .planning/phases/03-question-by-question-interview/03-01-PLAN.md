---
phase: 03
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/app/services/recording_service.py
  - backend/app/routers/recording.py
  - backend/app/main.py
autonomous: true
requirements:
  - VOICE-02
user_setup: []

must_haves:
  truths:
    - "User's voice recording is saved to the server after each question"
    - "Recordings are organized by session and question number"
    - "Upload happens silently without blocking the user"
  artifacts:
    - path: "backend/app/services/recording_service.py"
      provides: "Recording file save logic with session directory management"
      contains: "def save_recording"
    - path: "backend/app/routers/recording.py"
      provides: "POST /api/recording/upload multipart form-data endpoint"
      contains: "async def upload_recording"
    - path: "backend/app/main.py"
      provides: "Recording router registration"
      contains: "recording"
  key_links:
    - from: "backend/app/routers/recording.py"
      to: "backend/app/services/recording_service.py"
      via: "import save_recording"
      pattern: "from app.services.recording_service import save_recording"
    - from: "backend/app/main.py"
      to: "backend/app/routers/recording.py"
      via: "app.include_router"
      pattern: "recording.router"
---

<objective>
Create the backend recording upload endpoint for Phase 3. Users' voice answers are recorded via MediaRecorder on the frontend, then uploaded silently to the backend after each question completes.

Purpose: Provide the server-side storage for interview recordings, organized by interview session.
Output: Recording upload API endpoint + service + router registration.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/03-question-by-question-interview/03-CONTEXT.md
@.planning/phases/02-virtual-exam-room/02-CONTEXT.md

# Reference: existing backend patterns
@backend/app/services/guidance_service.py
@backend/app/routers/tts.py
@backend/app/main.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create recording service</name>
  <files>backend/app/services/recording_service.py</files>
  <read_first>
    - backend/app/services/guidance_service.py (existing service pattern)
    - backend/app/services/tts_service.py (existing service pattern)
  </read_first>
  <action>
    Create `backend/app/services/recording_service.py` with:

    - Module-level constant `RECORDINGS_DIR = "recordings"` (relative to backend working directory)
    - Function `save_recording(session_id: str, question_index: int, audio_data: bytes) -> str`:
      - Creates directory `recordings/{session_id}/` using `os.makedirs(exist_ok=True)`
      - Writes `audio_data` to `recordings/{session_id}/q{question_index}.webm`
      - Returns the absolute file path of the saved recording
      - Uses `pathlib.Path` for path construction (consistent with guidance_service import style)
    - Function `ensure_recordings_dir()` called at module level or on first save to create the base `recordings/` directory
    - Logging: log each successful save at INFO level with session_id, question_index, file size
    - Error handling: wrap file I/O and raise `RuntimeError` with descriptive message on failure
    - Use `import logging` and `logger = logging.getLogger("interview-simulator")` (matching tts_service.py pattern)
  </action>
  <verify>
    <automated>python3 -c "from app.services.recording_service import save_recording; print('import OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/services/recording_service.py` exists
    - `save_recording(session_id, question_index, audio_data)` is exported
    - Function creates `recordings/{session_id}/` directory with `os.makedirs(exist_ok=True)`
    - Function writes file at path `recordings/{session_id}/q{question_index}.webm`
    - Function returns the saved file path as a string
    - Imports include `logging`, `os`, `pathlib.Path`
  </acceptance_criteria>
  <done>Recording service created with save_recording function that writes audio data to session-organized directories.</done>
</task>

<task type="auto">
  <name>Task 2: Create recording router and register in main.py</name>
  <files>
    backend/app/routers/recording.py
    backend/app/main.py
  </files>
  <read_first>
    - backend/app/routers/tts.py (existing router pattern with APIRouter, Pydantic, Error handling)
    - backend/app/services/recording_service.py (created in Task 1)
  </read_first>
  <action>
    Create `backend/app/routers/recording.py`:

    - APIRouter with `tags=["recording"]`
    - `POST /api/recording/upload` endpoint
    - Accept multipart form-data with FastAPI `UploadFile` and `Form`:
      - `session_id: str = Form(...)` — non-empty string, UUID-like
      - `question_index: int = Form(...)` — non-negative integer
      - `audio: UploadFile = File(...)` — the recorded audio file (WebM/Opus)
    - Validation:
      - If `session_id` is empty or whitespace-only: raise HTTPException 422 with detail "session_id must be non-empty"
      - If `question_index < 0`: raise HTTPException 422 with detail "question_index must be non-negative"
      - If `audio.content_type` is set and is not `audio/webm` or `audio/opus`: log a warning (but accept the file — browsers may send different MIME types)
    - Read audio data via `await audio.read()`
    - Call `save_recording(session_id, question_index, audio_bytes)`
    - Return JSON `{ "status": "ok" }`
    - On any unexpected error, raise HTTPException 500 with detail including error message
    - Log successful saves at INFO level: "Recording saved: {path} (session={session_id}, q={question_index}, size={size} bytes)"

    Modify `backend/app/main.py`:

    - Add import: `from app.routers import recording`
    - Add after existing `app.include_router()` calls: `app.include_router(recording.router)`
    - Existing CORS config already allows POST and multipart — no CORS changes needed
  </action>
  <verify>
    <automated>
      python3 -c "
from app.routers.recording import router
# Verify POST endpoint exists
for route in router.routes:
    if hasattr(route, 'methods') and 'POST' in route.methods and '/api/recording/upload' in route.path:
        print('OK: POST /api/recording/upload found')
        break
else:
    print('FAIL: route not found')
    exit(1)
"
    </automated>
  </verify>
  <acceptance_criteria>
    - `backend/app/routers/recording.py` exists
    - POST `/api/recording/upload` endpoint accepts `session_id` (Form), `question_index` (Form), `audio` (UploadFile)
    - Validation rejects empty `session_id` (422) and negative `question_index` (422)
    - On success returns `{"status": "ok"}`
    - `backend/app/main.py` includes `recording.router`
    - No CORS changes needed (existing config allows POST)
  </acceptance_criteria>
  <done>Recording upload endpoint created and registered. Backend accepts multipart form-data uploads and saves recordings to session-organized directories.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → API | Untrusted multipart form-data crosses here |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01-01 | Denial of Service | POST /api/recording/upload | accept | No file size limit enforced (controlled deployment); uploads are single-user in v1. Accept risk. |
| T-03-01-02 | Spoofing | session_id | accept | session_id is generated client-side via crypto.randomUUID(). No auth in v1; user is sole operator. |
| T-03-01-03 | Information Disclosure | recording storage | accept | Recordings stored locally in `recordings/` dir. No PII in v1 scope. No external exposure. |
</threat_model>

<verification>
- `backend/app/services/recording_service.py` exists and exports `save_recording`
- `backend/app/routers/recording.py` exists with POST /api/recording/upload
- `backend/app/main.py` includes recording router
- Python import test: `python3 -c "from app.services.recording_service import save_recording; from app.routers.recording import router"`
</verification>

<success_criteria>
Backend recording upload endpoint is operational: accepts multipart form-data with session_id, question_index, and audio file; validates inputs; saves to `recordings/{session_id}/q{n}.webm`; returns `{ status: 'ok' }`.
</success_criteria>

<output>
After completion, create `.planning/phases/03-question-by-question-interview/03-01-SUMMARY.md`
</output>
